name: Render Auto Deploy

on:
  workflow_run:
    workflows: ["CI/CD Pipeline"]
    types:
      - completed
    branches: [main]

jobs:
  deploy:
    name: Deploy to Render
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Trigger Render deployment
        run: |
          echo "✅ CI/CD passed! Render will automatically deploy."
          echo "🔗 Service URL: https://chainquery-app.onrender.com"
          
      - name: Wait for deployment
        run: |
          echo "⏳ Waiting for Render to complete deployment..."
          echo "Check status at: https://dashboard.render.com"

  deployment-failed:
    name: CI Failed - No Deploy
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}

    steps:
      - name: Deployment blocked
        run: |
          echo "❌ CI/CD checks failed. Deployment blocked."
          echo "Fix the failing tests before deploying to production."
          exit 1
