{
  "name": "chainquery-ai-root",
  "version": "1.0.0",
  "description": "Root package to manage ChainQuery AI full-stack development",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && venv\\Scripts\\pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "test": "cd backend && venv\\Scripts\\pytest"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
